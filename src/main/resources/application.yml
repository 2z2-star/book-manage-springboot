server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_db?serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 888888
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 15
      minimum-pool-size: 5

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.test.bookserver.entity
  configuration:
    map-underscore-to-camel-case: true
